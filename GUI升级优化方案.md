# 萝卜盒 GUI 升级优化方案

> 视角：年轻用户（20-30 岁，天天用 VSCode / Warp / Raycast 这类工具的人）
> 范围：`luobobox/ui/` 全量（theme / widgets / main_window / wizard / tray / admin / usage_view）
> 状态：**提案，未动代码**

---

## TL;DR

现在的 GUI 是「工程师自用工具」的审美 —— **结构合理、功能完整、但缺少「想打开它」的理由**。三句话总结：

1. **信息架构按模块平铺**（7 个页签），而非按使用频率分组 → 每天要用的和一年用一次的一样大。
2. **没有全局动作入口** → 复制 Key、检查更新、重启网关分散在三个页签，全靠记忆找。
3. **反馈要么消失、要么没有** → Toast 6 秒自灭（长错误看不见），空状态是空表，零动效。

**建议顺序**：先修 1 个真 bug + 加键盘流（半天，立刻可感知）→ 命令面板（1 天，专业感最大单点）→ 主题引擎（1 天，视觉焕新）。

---

## 一、现状盘点（已逐文件核对）

### 1.1 结构

| 项 | 现状 |
|---|---|
| 页签 | **7 个平铺**：概览 / 管理台 / 客户端接入 / 日志 / 更新 / 设置 / **额度消耗** |
| 页签注册 | `main_window._build()` 用 `_tab_index` 键值表（**已去掉硬编码**，是好的改进） |
| 例外 | 额度消耗由 `app.py:252` 在 MainWindow **之后**二次 `addTab` → 索引 6，排在最末 |
| 窗口 | `resize(940, 720)`，`setMinimumSize(780, 560)` —— 4K 屏上是个火柴盒，且不能记忆上次尺寸 |
| 状态栏 | 只有 `statusBar().showMessage("就绪")`，之后几乎不更新 |

### 1.2 视觉

- **单一暗色主题**，硬编码在 `theme.py` 的 `stylesheet()` 里（261 行）。配色底子不差：
  `BG #17181A` / `SURFACE #242629` / `BORDER #34383D` / `ACCENT #E06A4A`（萝卜红）。
- 圆角 10px（卡片）/ 8px（表格）/ 7px（输入框），一致性好。
- **页签是纯文字，无图标**；按钮无图标；托盘菜单 14 项纯文字。
- **无动效**：唯一的动态视觉是 `StatusDot`（静态画一次）。
- 字体 `"Microsoft YaHei UI"` 13px 基准 / 等宽 `"Cascadia Mono"`。

### 1.3 组件库（`widgets.py`，316 行）

已有：`StatusDot` `Card` `KeyValue` `Pill` `CopyField` `BigButton` `Separator` `section_title` `Toast` `elide` `path_font`。

**缺的**：空状态、骨架屏/加载占位、确认对话框封装、图标系统、工具提示气泡、可折叠区块、搜索框、快捷键提示条。

### 1.4 首次向导（`wizard.py`，468 行）

4 步（欢迎 / 运行环境 / 客户端 / 启动方式），自绘 `StepDots` 步骤指示器 —— **这块是全项目最像"产品"的地方**，视觉上不用大改，只需补文案和动效。

### 1.5 🔴 发现一个真 bug（顺带）

`tray.py:175`：

```python
def _check_update(self) -> None:
    self.window.tabs.setCurrentIndex(3)   # ← 写死的 3
```

页签实际顺序是 `概览0 管理台1 客户端接入2 日志3 更新4 设置5`，所以托盘点「检查更新（萝卜盒 / 网关）」**会跳到「日志」页，而不是「更新」页**。

`main_window.py` 的注释里已经明确写过「以前把 1/2/3 写死，一插入新页签就会静默错位」—— **托盘这处是漏网的**。

---

## 二、诊断：年轻人视角的 7 个判据

| # | 判据 | 现状 | 判定 |
|---|---|---|---|
| 1 | 首屏 1 秒内回答「现在什么状态、我能点什么」 | 4 张卡片 + 1 个表格并列，状态在左上角一行小字 | ❌ 要读 |
| 2 | 高频动作不用翻页签 | 复制 Key 在概览、检查更新在更新、重启在页头 | ❌ 分散 |
| 3 | 键盘优先 | **零快捷键** | ❌ 缺失 |
| 4 | 反馈有存在感但不打扰 | Toast 6 秒自灭；错误信息会被吃掉 | ⚠️ 风险 |
| 5 | 视觉有性格、有生命感 | 配色不错，但零动效、零图标 | ⚠️ 平淡 |
| 6 | 空状态带路 | 凭证池 0 条 = 一张空表 | ❌ 缺失 |
| 7 | 屏幕越大越好看 | 固定 940×720，不记忆尺寸 | ❌ 浪费 |

---

## 三、方案

### P0 — 打磨层（约 0.5–1 天，零架构改动，立刻可感知）

| # | 项 | 做法 | 涉及文件 | 行数 |
|---|---|---|---|---|
| P0-1 | **修托盘跳错页签** | `setCurrentIndex(3)` → 新增 `MainWindow.goto_tab(key)` 公开方法，内部查 `_tab_index`；托盘改调它 | `tray.py` `main_window.py` | ~12 |
| P0-2 | **全局快捷键** | `QShortcut`：`Ctrl+1..7` 切页签 / `Ctrl+R` 重启网关 / `Ctrl+Shift+C` 复制 Key / `F5` 刷新健康 / `Ctrl+,` 进设置 / `Ctrl+K` 命令面板 | `main_window.py` | ~45 |
| P0-3 | **Toast 可固定** | `show_message(..., sticky=False)`；`level="error"` 默认 sticky；点击可展开全文 / 复制；右下角「×」手动关 | `widgets.py` | ~30 |
| P0-4 | **空状态组件** | 新增 `EmptyState(title, hint, action_text, on_action)`；接入 3 处：凭证池无账号、日志无记录、额度消耗无快照 | `widgets.py` `main_window.py` | ~60 |
| P0-5 | **一键复制接入包** | 「复制接入包」→ 拼一段 Markdown（四种地址 + Key + 一段示例 config），直接贴给同事 | `main_window.py` | ~25 |
| P0-6 | **大屏自适应 + 记忆尺寸** | 启动时 `availableGeometry()` 取 78%×82%，上限 1440×960；`closeEvent` 存 `QSettings`；卡片网格按宽度三档断点 | `main_window.py` | ~30 |
| P0-7 | **状态点呼吸动效** | 仅在 `running` 时启动 1.8s 透明度呼吸（`QPropertyAnimation` + `InOutSine`），停止即停 | `widgets.py` | ~25 |

**P0 合计 ≈ 230 行，不动任何业务逻辑。**

### P1 — 体验跃迁（约 2–3 天）

| # | 项 | 做法 | 价值 |
|---|---|---|---|
| P1-1 | **命令面板 `Ctrl+K`** | 一个 `QDialog`：`QLineEdit` + `QListWidget`，模糊匹配三类目标 —— 7 个页签 / 12 个动作（启动·停止·重启·复制 Key·复制接入包·打开数据目录·打开日志·体检·重打补丁·签到·检查更新·迁移数据目录）/ 外观切换。↑↓ 选择、Enter 执行、Esc 关闭 | **专业感的最大单点提升**。年轻人判断一个工具"是否现代"，第一眼看的是有没有命令面板 |
| P1-2 | **主题引擎** | `theme.py` 抽出 `PALETTES = {"dark": {...}, "light": {...}}` 与 `ACCENTS = {"萝卜红": "#E06A4A", "青柠": "#9BC53D", "电紫": "#7C5CFF", "湖蓝": "#2E9BD6"}`；`stylesheet(palette, accent, scale)` 接收参数。设置页新增「外观」卡片：暗 / 亮 / 跟随系统 + 4 个强调色 + 字号 100%/115%/130%，`app.setStyleSheet()` 热重载不重启 | 亮色是刚需（白天办公用暗色刺眼）；换强调色 = 零成本个性化 |
| P1-3 | **首屏英雄区** | 概览页顶部换成 Hero：左侧 96px 自绘状态环（运行时长 + 状态），中间大字状态 + 一行副标题 + 状态 Pill 组，右侧两个主按钮。**卡片下沉到第二屏** | 判据 #1 |
| P1-4 | **导航分组** | 方案 A（低成本）：保留顶部页签，但只留 4 个高频（概览 / 管理台 / 客户端接入 / 更新），其余进右侧「⋯」溢出菜单。方案 B（高成本，更像现代工具）：左栏 56px 图标竖导航 + 底部「设置」 | 7 → 4，认知负担减半。**建议先做 A，B 留给 P2** |
| P1-5 | **动效语言** | 新建 `ui/motion.py` 统一：卡片进场 180ms 淡入 + 上移 8px；Toast 250ms 滑入滑出；页签切换 120ms 交叉淡入；按钮按下 `scale 0.98`。全部 `QEasingCurve.OutCubic` | 判据 #5。动效是"贵"和"廉价"的分界线 |
| P1-6 | **托盘年轻化** | 菜单项加 `QIcon`（内联 SVG，无需外部资源）；首项显示 `● 运行中 · 8789`；加「复制接入包」；「检查更新」加新版本徽标 | 托盘是常驻入口，现在最朴素 |
| P1-7 | **日志页升级** | 级别色块 + 关键字高亮（ERROR 红底 / WARN 左边框）+ 正则过滤 + 一键导出 `.log` + 双击整行复制 + 「跟随尾部」开关 | 排障效率直接翻倍 |
| P1-8 | **额度消耗可视化** | 现在是表格 → 改成 7/30 天柱状 + 今日环比 + 模型分布环形（`QPainter` 自绘，**不引第三方库**） | 数据直觉 > 数字表格 |

### P2 — 品牌记忆（按需，不着急）

- 玻璃拟态卡片（QSS 半透明 + 1px 高光边）
- 明暗切换时的 250ms 全局过渡
- 连续签到 streak（火苗 + 天数）+ 里程碑徽章
- 自定义低透明度背景纹理
- 页签图标系统（内联 SVG → `QIcon`，替换纯文字页签）
- 「专注布局」：一键隐藏所有高级页签，只留概览 + 管理台

---

## 四、关键不变量（改 GUI 时不能破坏）

| # | 不变量 | 原因 |
|---|---|---|
| 1 | **`_tab_index` 键值注册表** | 新增页签一律走它，**绝不再写死数字**（托盘那处就是反面教材） |
| 2 | **`theme.py` 不许写 `QWidget { background-color }`** | 会命中所有子控件含 `QLabel`，界面刷出色带（源码里有注释警告） |
| 3 | 所有耗时操作走 `ctx.run_task` 到后台线程 | UI 线程只做渲染 |
| 4 | `QTimer.singleShot(0, _refresh_disk_info)` | 递归扫 2 万个文件体积，直接跑会卡住建窗 |
| 5 | **Toast 只放首行、完整多行错误灌备注框** | 网络失败的 21 秒长报错不能被 toast 截断 |
| 6 | `_fmt_credits()` 必须容错 dict | 网关的 `credits` 可能是对象，早期直接 `f"{value:,}"` 导致凭证表整片空白 |
| 7 | 设置页保存链路 `_save_settings` / `_reload_settings` 的键名 | 新增「外观」卡片要能并发 `config.set/save` |

---

## 五、任务清单（按建议执行顺序）

| 序 | 任务 | 优先级 | 文件 | 估行 | 可验证方式 |
|---|---|---|---|---|---|
| 1 | 修托盘跳错页签 + `goto_tab()` | P0 | tray / main_window | 12 | 新增断言：托盘动作后 `tabs.currentIndex() == _tab_index["update"]` |
| 2 | 全局快捷键 | P0 | main_window | 45 | 断言 `QShortcut` 数量 + 绑定表 |
| 3 | Toast sticky | P0 | widgets | 30 | 现有 `gui_smoke` 通过 |
| 4 | EmptyState 组件 + 3 处接入 | P0 | widgets / main_window | 60 | 截图比对 |
| 5 | 复制接入包 | P0 | main_window | 25 | 断言拼串含 4 地址 + Key |
| 6 | 大屏自适应 + 记忆尺寸 | P0 | main_window | 30 | 断言上限 1440×960 |
| 7 | 状态点呼吸动效 | P0 | widgets | 25 | 目视 |
| 8 | 命令面板 Ctrl+K | P1 | **新文件** `ui/palette.py` | ~180 | 单测：模糊匹配 + 动作分发表 |
| 9 | 主题引擎 + 外观卡片 | P1 | theme / main_window | ~200 | 4 强调色 × 2 明暗 = 8 组截图 |
| 10 | 首屏英雄区 | P1 | main_window | ~140 | 截图 |
| 11 | 导航分组（方案 A） | P1 | main_window | ~60 | 断言 4 + 3 |
| 12 | 动效语言 | P1 | **新文件** `ui/motion.py` | ~120 | 目视 |
| 13 | 托盘图标化 | P1 | tray | ~80 | 截图 |
| 14 | 日志页高亮/导出 | P1 | main_window | ~110 | 造一条 ERROR 日志验证 |
| 15 | 额度消耗可视化 | P1 | usage_view | ~150 | 截图 |

**总计约 1,270 行**（P0 ≈ 230 / P1 ≈ 1,040）。

---

## 六、建议先做这 3 件（ROI 排序）

1. **P0-1 + P0-2**（半天）—— 一个真 bug + 一套键盘流。用户第二天就能感觉到"快了"。
2. **P1-1 命令面板**（1 天）—— 年轻用户对"现代工具"的判定几乎全在这一个组件上，投入产出比最高。
3. **P1-2 主题引擎**（1 天）—— 亮色 + 可换强调色，是"焕新"最省的路径（不动布局就能看起来换了个软件）。

做完这 3 件，观感提升大约等于「重做一遍 UI」的 70%，成本只有 20%。

---

## 七、验收要求（不许破坏现有资产）

- 现有 **218 项断言全绿**：`selftest` 80 / `appupdater_selftest` 20 / `updater_preserve_selftest` 19 / `webui_selftest` 34 / `net_selftest` 65
- `tests/gui_smoke.py` 通过（**cwd 必须是 `luobobox/`**）
- 打包自检 `LuoboBox.exe --selftest` 全 PASS
- 新增 GUI 断言集中在 `tests/gui_layout_selftest.py`（页签注册表 / 快捷键表 / 主题参数 / 托盘分发），保持"无 Qt 依赖也能跑"的风格
- 发版后跑 `python tests/e2e_release_asset.py <tag>`
